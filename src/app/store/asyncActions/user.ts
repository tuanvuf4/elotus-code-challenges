import { createAsyncThunk } from '@reduxjs/toolkit'
// import { logout } from 'src/app/common/services/UserService'
// 
// export const logoutUser = createAsyncThunk('users/logout', async (refreshToken: string) => {
//   return await logout(refreshToken)
// })
